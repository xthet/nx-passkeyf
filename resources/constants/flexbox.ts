export const fxbx = [
	function ({ addUtilities }: { addUtilities: any }) {
		const new_utils = {
			".fl-tl": {
				display: "flex",
				alignItems: "flex-start",
				justifyContent: "flex-start",
			},
		}

		addUtilities(new_utils, ["responsive, hover"])
	},

	function ({ addUtilities }: { addUtilities: any }) {
		const new_utils = {
			".fl-tc": {
				display: "flex",
				alignItems: "flex-start",
				justifyContent: "center",
			},
		}

		addUtilities(new_utils, ["responsive, hover"])
	},

	function ({ addUtilities }: { addUtilities: any }) {
		const new_utils = {
			".fl-tr": {
				display: "flex",
				alignItems: "flex-start",
				justifyContent: "flex-end",
			},
		}

		addUtilities(new_utils, ["responsive, hover"])
	},

	function ({ addUtilities }: { addUtilities: any }) {
		const new_utils = {
			".fl-cl": {
				display: "flex",
				alignItems: "center",
				justifyContent: "flex-start",
			},
		}

		addUtilities(new_utils, ["responsive, hover"])
	},

	function ({ addUtilities }: { addUtilities: any }) {
		const new_utils = {
			".fl-cc": {
				display: "flex",
				alignItems: "center",
				justifyContent: "center",
			},
		}

		addUtilities(new_utils, ["responsive, hover"])
	},

	function ({ addUtilities }: { addUtilities: any }) {
		const new_utils = {
			".fl-cr": {
				display: "flex",
				alignItems: "center",
				justifyContent: "flex-end",
			},
		}

		addUtilities(new_utils, ["responsive, hover"])
	},

	function ({ addUtilities }: { addUtilities: any }) {
		const new_utils = {
			".fl-bl": {
				display: "flex",
				alignItems: "flex-end",
				justifyContent: "flex-start",
			},
		}

		addUtilities(new_utils, ["responsive, hover"])
	},

	function ({ addUtilities }: { addUtilities: any }) {
		const new_utils = {
			".fl-bc": {
				display: "flex",
				alignItems: "flex-end",
				justifyContent: "center",
			},
		}

		addUtilities(new_utils, ["responsive, hover"])
	},

	function ({ addUtilities }: { addUtilities: any }) {
		const new_utils = {
			".fl-br": {
				display: "flex",
				alignItems: "flex-end",
				justifyContent: "flex-end",
			},
		}

		addUtilities(new_utils, ["responsive, hover"])
	},

	function ({ addUtilities }: { addUtilities: any }) {
		const new_utils = {
			".fl-sb": {
				justifyContent: "space-between",
			},
		}

		addUtilities(new_utils, ["responsive, hover"])
	},

	function ({ addUtilities }: { addUtilities: any }) {
		const new_utils = {
			".fl-sa": {
				justifyContent: "space-around",
			},
		}

		addUtilities(new_utils, ["responsive, hover"])
	},

	function ({ addUtilities }: { addUtilities: any }) {
		const new_utils = {
			".fl-c": {
				flexDirection: "column",
			},
		}

		addUtilities(new_utils, ["responsive, hover"])
	},

	function ({ addUtilities }: { addUtilities: any }) {
		const new_utils = {
			".fl-r": {
				flexDirection: "row",
			},
		}

		addUtilities(new_utils, ["responsive, hover"])
	},
]
